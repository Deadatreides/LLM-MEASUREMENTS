# REPORT — K1: контора Кана. Декомпозиция контекста работает, эмерджентности нет

Пред-регистрация: `PROTOCOL_K1.md`. Пороги не двигались. 720 живых
вызовов, 614 с. База (полный контекст) — с диска, 0 новых вызовов.

---

## 1. Сначала — опровержение собственной заявки

Пред-регистрированный порог `K1-emergence` был сформулирован мной
**неудачно**: «собранный ответ верен, а ни одна модель не взяла набор
целиком **на полном контексте**». По нему вышло **34 из 40** — и это
артефакт: сравнивался полный конвейер (блоки + ВДП + консенсус +
декодирование) против **сырого** извлечения одиночной модели. Разные
условия по обе стороны.

Корректное сравнение — одиночная модель через **идентичный** конвейер:

| | |
|---|---|
| собранный конвейер, 6 моделей | **0.900** |
| лучшая одиночная через тот же конвейер (`qwen3-1.7b`) | **0.850** |
| вклад роя | **+0.050** |
| оракул «хоть одна из 6» через тот же конвейер | 0.975 |
| **строгая эмерджентность** | **0** |

**Эмерджентности нет.** Собранный ответ всегда лежит внутри оракула
«хоть одна модель справилась». Число 34 оставлено в
`metrics/k1_result.json` как есть, рядом с 0 — старое значение не
удаляется, дефект формулировки виден.

Это уже вторая проверка эмерджентности, дающая ноль (G1 дал ноль
структурно). Единственный случай, где она была измерена — E0, где сборка
шла **по полям одного совместного ответа** (0.870 против оракула 0.840).
Композиция по БЛОКАМ КОНТЕКСТА её не даёт.

## 2. Что подтвердилось — и это главное

### 2.1 Позиционный распад полноты устранён

`probe_lost_middle.py` (0 GPU) нашёл на уже собранных данных: полнота
падает **монотонно** от 0.609 на позиции 0 до 0.214 на позиции 17.
Это не «потеря в середине» (U-образного провала нет), а распад
нахождения — модель всё чаще говорит «нет» на поздние записи.

Разбиение на 3 блока по 6 записей:

| | полный контекст | блоки Кана |
|---|---|---|
| recall, голова (поз. 0–5) | 0.508 | 0.595 |
| recall, хвост (поз. 12–17) | **0.304** | **0.574** |
| **разрыв голова−хвост** | **0.204** | **0.021** |

**Разрыв схлопнулся в десять раз.** Порог `K1-flat` был ≤0.12 —
получено 0.021. Позиция записи в контексте перестала влиять на то,
найдут ли её. Отдельные позиции хвоста выросли на +0.30…+0.375.

### 2.2 Декомпозиция помогает СЛАБЫМ моделям сильнее, чем сильным

exact-set после декодирования, полный контекст → блоки:

| модель | полный | блоки | прирост |
|---|---|---|---|
| qwen2.5-coder-1.5b | 0.175 | **0.700** | **+0.525** |
| llama-3.2-1b | 0.400 | **0.725** | **+0.325** |
| internvl3-2b | 0.575 | 0.825 | +0.250 |
| qwen3-1.7b | 0.725 | 0.850 | +0.125 |
| gemma-3-1b | 0.000 | 0.050 | +0.050 |
| smollm2-1.7b | 0.000 | 0.000 | 0 |

Выросли все, кто вообще работает, и **чем слабее модель, тем больше
прирост**. Это ровно тезис Кана в численном виде: правильное деление
работы поднимает посредственностей до уровня, где их труд складывается.
`qwen2.5-coder` вырос вчетверо и почти догнал лидера.

### 2.3 Итог по задаче

| F2 test, n=40 | |
|---|---|
| exact-set, полный контекст + декодирование (E4) | 0.750 |
| **exact-set, блоки + сборка + декодирование** | **0.900** |
| финал ДА/НЕТ | **1.000** (whole-task `r_m*` = 0.525) |
| ВДП отбросил галлюцинаций (id вне своего блока) | 5 |

Порог `K1-set` был ≥0.85 — получено 0.900, `CONFIRMED`.

## 3. Что из схемы Кана реально несло нагрузку

| элемент Кана | реализовано | измеренный вклад |
|---|---|---|
| деление на дисциплины (блоки) | детерминированно, по схеме | **основной: +0.150 к exact-set, разрыв 0.204→0.021** |
| исполнители | 6 моделей на блок | +0.050 сверх лучшей одиночной |
| ВДП (внутридисциплинарная) | id вне блока → отброс | 5 галлюцинаций поймано |
| нормоконтроль | валидация формата | 0 вызовов, ошибок формата нет |
| **МДП (междисциплинарная)** | реализована | **вырождена: блоки F2 независимы, проверять на швах нечего** |
| консультация главспеца | E6-механизм | не потребовалась: 0.900 достигнуто без неё |
| 3 ревизии | margin-стробирование | не потребовалось |

**Честно: работала одна дисциплина Кана из семи — деление на блоки.**
Остальное либо вырождено на этой задаче (МДП), либо не понадобилось
(консультация, ревизии), либо дало малый вклад (исполнители-рой, ВДП).
Выдавать K1 за подтверждение схемы Кана целиком нельзя.

## 4. Non-claims

- **Эмерджентности нет** (строгая мера — 0). Заявка в §1 опровергнута
  мной же на том же прогоне.
- МДП на F2 **не проверена по существу** — блоки независимы. Нужна
  задача с зависимостями между блоками; это следующий полигон.
- `STRUCTURE_DET = true`: декодирование, ВДП, нормоконтроль
  детерминированы. `filter_det = 1.000` не побит (K1: 0.900).
- Финал 1.000 при exact-set 0.900 — мягкость булева ответа, честная
  величина 0.900.
- Размер блока 6 выбран из измеренного профиля полноты (позиции 0–5 —
  зона высокой полноты), но **не оптимизировался**; 3 и 9 не пробовались.
- 40 задач, одна семья, один сплит.

## 5. Куда это ведёт

Итог трёх последних пакетов складывается в одну формулировку:

> **Выигрыш даёт декомпозиция — контекста (K1), структуры ответа (E4),
> вопроса (E6). Пул даёт от 0 до +0.08 сверху и никогда не даёт
> эмерджентности.**

Эмерджентность измерена ровно один раз — в E0, где сборка шла по ПОЛЯМ
одного совместного ответа. Ни мода по финальному числу (G1), ни
композиция по блокам контекста (K1) её не воспроизвели. Если она вообще
достижима, то только там, где **разные модели вносят разные ЧАСТИ
одного ответа**, а не разные мнения о целом и не разные куски входа.
