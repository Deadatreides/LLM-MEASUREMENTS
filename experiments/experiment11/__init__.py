"""experiment11 — collision -> action -> outcome, чистый стенд.

НЕ зависит от arch1/ (Mycelium): не импортирует arch1.src.*, не строит
claim-граф, не реализует state-machine/оркестратор. Состояние коллизии —
плоский dict, не версионированный объект.

Переиспользует только низкоуровневый стенд генерации
(experiment9/configs/{llm_client,env_fix,chat_render}.py) — то же самое,
что во всех experiment1-9, задолго до Mycelium.

Вопрос эксперимента (единственный): может ли систематическое
распределение вычисления по разным операторам после коллизии дать
больше решённых задач на единицу токенового бюджета, чем повторное
сэмплирование одного оператора?
"""
