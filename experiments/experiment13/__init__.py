"""experiment13 — collision -> representation shift -> outcome.

Проверяет: помогает ли после коллизии смена СПОСОБА ПРЕДСТАВЛЕНИЯ/
РАЗБИЕНИЯ задачи (не оператора-модели). Три режима на одном и том же
замороженном collision-state, ОДНОЙ и той же моделью (без CHANGE_MODEL
-- намеренно, чтобы не смешивать факторы с программой экспериментов
10-12): R0 (REGENERATE, контроль) / R1 (REFRAME) / R2 (REDECOMPOSE).

Не Mycelium: не реализует PTG, роутер, bandit, циклы, автоматическую
архитектуру. Переиспользует датасет и collision-state Эксперимента 12
(`experiment12/runs12/phase0_full_exp12-v1.json`) -- не генерирует
новые исходные попытки.
"""
