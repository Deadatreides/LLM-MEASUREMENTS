"""20 задач с ветвящимся DAG для ARCH-1 (этап 6/7).

Данные (TASKS, DEPENDENCY_EDGES) перенесены буквально из
experiment9/tasks/schema9.py — «проверенная арифметика и граф»,
WORK_PLAN.md §3 «Этап 6»: задачи не придумывать заново.

Промпты для GENERATE (generation_prompt) адаптированы, а не перенесены:
experiment9 генерировал весь артефакт одним вызовом со структурированными
заголовками (prompts9.D_TEMPLATE) и парсил секции постфактум. ARCH-1
требует раздельного REGENERATE по claim'у (PATCH исключён, WORK_PLAN
§8) — поэтому каждый claim генерируется отдельным GENERATE-действием со
своей инструкцией (по формулировкам D_TEMPLATE, но по одной на claim).
"""
